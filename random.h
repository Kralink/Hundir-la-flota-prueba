#ifndef RANDOM
#define RANDOM

//Precondición: *a >= 1 ; *c >= 'A'
//Postcondicion: guarda en a y c un num y caracter aleatorios entre 1 y max y entre A y max, formando una coord
void rand_cord(int *a, char *c, int max);

#endif // RANDOM_H_INCLUDED
